# PDG-for-Verilog
Utilizes PyVerilog to parse through a verilog module to build a Program Dependence Graph

Ensure modules are installed using: pip install pyverilog networkx matplotlib

To use: Add verilog module to same directory, run PDG.py, and input the file name.
